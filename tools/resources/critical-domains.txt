# Critical domains that receive static JS injection bundles.
# One domain per line. Lines starting with # and blank lines are ignored.
# The bundle for domain D will match *://D/* and *://*.D/* in the manifest.
youtube.com
