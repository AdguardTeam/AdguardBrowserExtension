.limit-warning {
    font-size: 16px;
    line-height: 1.3;
    padding: 4px 16px 16px;
    color: var(--text-links-attention-link-default);

    a {
        color: var(--text-links-attention-link-default);
    }

    &--link {
        color: inherit;
        margin-left: 4px;
    }
}
