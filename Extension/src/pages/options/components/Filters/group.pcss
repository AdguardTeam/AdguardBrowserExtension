.group {
    cursor: pointer;

    &:hover .group__title {
        text-decoration: underline;
    }

    &--disabled {
        & .setting__icon {
            color: var(--gray400);
        }
    }
}
