.group {
    cursor: pointer;

    &--disabled {
        & .setting__icon {
            color: var(--stroke-icons-gray-icons-default);
        }
    }

    &__checkbox {
        @media (min-width: 640px) {
            border-radius: 8px;
        }
    }
}
