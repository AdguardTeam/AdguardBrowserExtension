.about {
    padding: 16px 16px 40px 16px;
    font-weight: 400;

    &__title {
        font-weight: 700;
        font-size: 20px;
        margin-bottom: 8px;
    }

    &__version {
        color: var(--gray4a);
        font-size: 16px;

        @media (prefers-color-scheme: dark) {
            color: var(--gray300);
        }
    }

    &__copyright {
        font-size: 16px;
        color: var(--gray4a);
        margin-bottom: 32px;

        @media (prefers-color-scheme: dark) {
            color: var(--gray400);
        }
    }
}

.light-mode {
    .about {
        &__version {
            color: var(--gray4a);
        }

        &__copyright {
            color: var(--gray4a);
        }

        &__menu-item {
            color: var(--gray4a);
        }
    }
}

.dark-mode {
    .about {
        &__version {
            color: var(--gray300);
        }

        &__copyright {
            color: var(--gray400);
        }
    }
}
