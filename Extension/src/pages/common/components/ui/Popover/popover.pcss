.popover {
    display: inline-block;

    &--coming-soon {
        cursor: help;
        display: flex;

        /* FIXME(Slava): remove when companiesdb stats is implemented for mv3 */
        & > .tabs__tab {
            width: 160px;
        }
    }
}
