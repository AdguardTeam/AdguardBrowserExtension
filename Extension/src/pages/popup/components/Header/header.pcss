.popup-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 50px;
    padding: 0 16px;

    &__logo {
        width: 113px;
        height: 25px;
    }

    &__buttons {
        display: flex;
    }
}
