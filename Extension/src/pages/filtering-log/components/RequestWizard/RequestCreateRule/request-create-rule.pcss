.radio-button-label, .checkbox-label {
    margin-bottom: 18px;
}

.radio-button {
    min-width: 17px;
}

.request-modal {
    &__rule-text {
        /* todo - extract fonts and add fallbacks */
        /* todo - multiline input */
        font-family: PTMono-Regular;
        font-size: 16px;
        color: var(--gray900);
        width: 100%;
        border: none;
        margin-bottom: 24px;
    }
}

.patterns {
    /* todo - extract fonts and add fallbacks */
    font-family: PTMono-Regular;
    font-size: 16px;
    color: var(--gray900);
}

.options {
    /* todo - extract fonts and add fallbacks */
    font-family: Roboto-Regular;
    font-size: 16px;
    color: var(--gray900);
}
